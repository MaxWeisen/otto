{
  "name": "otto",
  "version": "0.0.0",
  "private": true,
  "description": "Otto monorepo — Go API + Next.js frontend",
  "scripts": {
    "commit": "cz",
    "dev": "concurrently -n db,api,web -c blue,green,magenta \"docker compose up db\" \"cd backend && go run ./cmd/otto\" \"cd frontend && pnpm dev\""
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "devDependencies": {
    "commitizen": "^4.3.1",
    "concurrently": "^10.0.3",
    "cz-conventional-changelog": "^3.3.0"
  }
}
